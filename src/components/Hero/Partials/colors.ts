/**
 * Three.js hex colors for the hero 3D scene.
 *
 * Mapping to Tailwind / CSS variables:
 *   whiteColor    0xcab2b8  — pinkish-gray (3D-only tint, no CSS equivalent)
 *   whiterColor   0xe4e4e4  — Tailwind `white` / --color-white
 *   orangeColor   0x972b1a  — Tailwind `orange-dark` / --color-orange-dark
 *   orangerColor  0x993838  — warmer orange (3D-only variant)
 *   primaryBlue   0x00171d  — close to Tailwind `primary-blue` (#001a25)
 *   raspberryColor 0x801834 — Tailwind `raspberry` / --color-raspberry
 */
export const colors = {
  whiteColor: 0xcab2b8,
  whiterColor: 0xe4e4e4,
  orangeColor: 0x972b1a,
  orangerColor: 0x993838,
  primaryBlue: 0x00171d,
  raspberryColor: 0x801834,
};
